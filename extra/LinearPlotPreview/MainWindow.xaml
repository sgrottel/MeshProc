<Window x:Class="LinearPlotPreview.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:LinearPlotPreview"
		xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
		mc:Ignorable="d"
		Title="MeshProc - Linear Plot Preview" Height="450" Width="800" Loaded="Window_Loaded">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="2*" MinWidth="50" />
			<ColumnDefinition Width="5" />
			<ColumnDefinition Width="3*" MinWidth="50" />
		</Grid.ColumnDefinitions>

		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="auto" />
				<RowDefinition Height="auto" />
				<RowDefinition Height="*" />
				<RowDefinition Height="auto" />
			</Grid.RowDefinitions>

			<StackPanel Orientation="Horizontal" Background="{DynamicResource {x:Static SystemColors.MenuBarBrushKey}}">
				<Button Margin="4,1,4,2" Padding="8,1" Click="ButtonPlot_Click">Plot</Button>
			</StackPanel>

			<TextBlock Grid.Row="1"
				Margin="4,0"
				FontFamily="Consolas"
				>function()<LineBreak />{</TextBlock>

			<TextBox Grid.Row="2"
				x:Name="Code"
				VerticalScrollBarVisibility="Visible"
				HorizontalScrollBarVisibility="Visible"
				AcceptsReturn="True"
				AcceptsTab="True"
				FontFamily="Consolas"
				></TextBox>

			<TextBlock Grid.Row="3"
				Margin="4,0"
				FontFamily="Consolas"
				>}</TextBlock>
		</Grid>

		<GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />

		<ScottPlot:WpfPlot Grid.Column="2" x:Name="WpfPlot1" />
	</Grid>
</Window>
