<Window x:Class="PlySelectorReceiver.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:PlySelectorReceiver"
		mc:Ignorable="d"
		Title="PlySelectorReceiver" Height="450" Width="800" Closing="Window_Closing" Loaded="Window_Loaded">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="2*" MinHeight="50" />
			<RowDefinition Height="5" />
			<RowDefinition Height="3*" MinHeight="50" />
			<RowDefinition Height="auto" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" MinWidth="50" />
			<ColumnDefinition Width="5" />
			<ColumnDefinition Width="*" MinWidth="50" />
		</Grid.ColumnDefinitions>

		<GridSplitter Grid.Column="1" Grid.RowSpan="3" Width="5" HorizontalAlignment="Stretch" />
		<GridSplitter Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Height="5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />

		<!-- Templates list -->
		<Grid Grid.Column="0" Grid.Row="0">
			<Grid.RowDefinitions>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<StackPanel Orientation="Horizontal" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
				<Button Margin="2" Click="LoadTemplatesButton_Click">Reload</Button>
				<Button Margin="2" Click="SaveTemplatesButton_Click">Save</Button>
				<Button Margin="2" Click="AddTemplateButton_Click">Add</Button>
				<Button Margin="2" Click="DeleteTemplatesButton_Click">Delete</Button>
			</StackPanel>

			<ListBox x:Name="TemplateList" Grid.Row="1" SelectionChanged="TemplateList_SelectionChanged">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal">
							<CheckBox IsChecked="{Binding IsActive}" Margin="0,0,4,0" />
							<TextBlock>
								<Run Text="{Binding Name}" />
								<Run Text="(" /><Run Text="{Binding SelectionType}" /><Run Text=")" />
							</TextBlock>
						</StackPanel>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>

		</Grid>

		<!-- Template Preview -->
		<Grid x:Name="TemplatePreview" Grid.Column="2" Grid.Row="0">
			<Grid.RowDefinitions>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="auto" MinWidth="50" />
				<ColumnDefinition Width="*" MinWidth="50" />
			</Grid.ColumnDefinitions>

			<StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
				<Button Margin="2" Click="StoreTemplateButton_Click">Store</Button>
			</StackPanel>
			<TextBlock Grid.Row="1" Padding="4,1" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">Name</TextBlock>
			<TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name}" />
			<TextBlock Grid.Row="2" Padding="4,1" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">Selection Type</TextBlock>
			<TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectionType}" />
			<TextBox Grid.Row="3"
				Grid.ColumnSpan="2"
				Text="{Binding Text}"
				AcceptsReturn="True"
				AcceptsTab="True"
				HorizontalScrollBarVisibility="Visible"
				VerticalScrollBarVisibility="Visible"
				TextWrapping="NoWrap"/>
		</Grid>

		<!-- Receiving list-->
		<Grid Grid.Column="0" Grid.Row="2">
			<Grid.RowDefinitions>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<StackPanel Orientation="Horizontal" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
				<Button Margin="2" Click="ButtonClearReceivedSelectionsList_Click">Clear</Button>
				<Button Margin="2" Click="ButtonReloadLastSelections_Click">Reload last</Button>
				<Button Margin="2" Click="ButtonLoadSelectionsData_Click" >Load</Button>
				<Button Margin="2" Click="ButtonSaveSelectionsData_Click" >Save</Button>
				<Button Margin="2" Click="ButtonDeleteSelectedSelections_Click">Delete</Button>
				<Button Margin="2" Click="ButtonCompileSelectedReceivedSelection_Click">Compile</Button>
			</StackPanel>

			<ListBox Grid.Row="1" x:Name="ReceivedSelectionsList" SelectionMode="Extended" />

		</Grid>

		<!-- Result text -->
		<Grid Grid.Column="2" Grid.Row="2">
			<Grid.RowDefinitions>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<StackPanel Orientation="Horizontal" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
				<Button Margin="2" Click="ButtonClearResultsText_Click">Clear</Button>
				<Button Margin="2" Click="ButtonCopyResultsText_Click">Copy</Button>
			</StackPanel>

			<TextBox x:Name="ResultsText"
					Grid.Row="1"
					VerticalScrollBarVisibility="Visible"
					HorizontalScrollBarVisibility="Visible"/>
		</Grid>

		<ContentControl Grid.Row="3" Grid.ColumnSpan="3" MouseDoubleClick="ContentControlStatusText_MouseDoubleClick">
			<TextBlock x:Name="StatusText" Text="" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Padding="4,2" TextWrapping="NoWrap" />
		</ContentControl>

	</Grid>
</Window>
